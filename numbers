1)pattern printing

#include <iostream>
using namespace std;
int main()
{
    int i, j, var = 'A';
    for (i = 5; i >= 1; i--) {
        for (j = 0; j < i; j++)
            printf("%c ", (var + j));
        printf("\n");
    }
    return 0;
}
```

**Output:**
```
A B C D E 
A B C D 
A B C 
A B 
A 

2)Factorial of a Number 

Compute the factorial of a number N (1 ≤ N ≤ 20). 
Input: 
5 
Output: 
120 

#include <iostream> 
//#include <string> 
//#include <algorithm> 
using namespace std; 
int fact(int n) 
{ 
} 
if (n==0 || n==1) 
return 1; 
else 
return n*fact(n-1); 
int main() 
{ 
} 
int n; 
cin>>n; 
cout<<fact(n);

3)Sum of Digits 

Find the sum of digits of an integer. 
Input: 
123 
Output: 
6 

#include <iostream> 
//#include <string> 
//#include <algorithm> 
using namespace std; 
int main() 
{ 
} 
int n,m; 
cin>>n; 
int sum=0; 
while(n>0) 
{ 
} 
m=n%10; 
sum=sum+m; 
n=n/10; 
cout<<sum; 
return 0;

4) Fibonacci Series 

Print the first N terms of the Fibonacci sequence. 
Input: 
5 
Output: 
0 1 1 2 3 
  
#include <iostream> 
//#include <string> 
//#include <algorithm> 
using namespace std; 
 
int main() 
{ 
  int n; 
  cin>>n; 
  int f=0,s=1; 
  int sum; 
  cout<<f<<" "<<s<<" "; 
  for (int i=3;i<=n;i++) 
  { 
     sum=f+s; 
     cout<<sum<<" "; 
     f=s; 
     s=sum; 
  } 
  return 0; 
} 
  
5)Armstrong Number

 Check whether a number is an Armstrong number. 
 Input: 
 153 
 Output: 
 Yes 

#include <iostream> 
//#include <string> 
//#include <algorithm> 
#include <cmath>  //pow() 
using namespace std; 
int cou(int n) 
{ 
} 
int count=0; 
while (n!=0) 
{ 
} 
n=n/10; 
count++; 
return count; 
int main() 
{ 
int n; 
cin>>n; 
int ip=n; 
int sum=0; 
int count=cou(n); 
while (n!=0) 
{ 
int d=n%10; 
sum=sum+pow(d,count); 
n=n/10; 
} 
cout<<sum<<endl; 
if (ip==sum) 
cout<<"yes"; 
else 
cout<<"no"; 
return 0; 
} 

6)You are given a positive integer N. 
In one step, you can do any of the following: 
1. Subtract 1 from N 
2. If N is divisible by 2, divide N by 2 
3. If N is divisible by 3, divide N by 3 
Find the minimum number of steps required to reduce N to 1. 
Input Format:- A single integer N (1 <= N <= 10^6) 
Output Format:- A single integer - the minimum steps to reduce N to 1. 
Example: 
Input: 
10 
Output: 
3 
Explanation: 
10 -> 9 -> 3 -> 1 (3 steps) 

#include <iostream> 
#include <climits> 
#include <algorithm> 
using namespace std; 
int minsteps(int n) 
{ 
} 
if (n==1) 
return 0; 
int ans1=minsteps(n-1); 
int ans2=INT_MAX; 
int ans3=INT_MAX; 
if (n%2==0) 
ans2= minsteps(n/2); 
if (n%3==0) 
ans3= minsteps(n/3); 
return 1+min({ans1,ans2,ans3}); 
int main() 
{ 
int n; 
cin>>n; 
cout<<minsteps(n); 
}

7)FRIENDLY PAIR

For two numbers num1 and num2 
Create two variables, sum1 and sum2 
Find the sum of divisors for num1 and num2 and assign the respective sums to sum1 
and sum2 variables 
Find the ratio of sum1/num1 and sum2/num2 
If both ratios are same then these are friendly pair numbers 
s i/p: 
6, 28 
s o/p: 
friendly pair 

#include <iostream> 
using namespace std; 
int sumofdivisors(int n) 
{ 
} 
int total=0; 
for (int i=1;i<=n;i++) 
if (n%i==0) 
total+=i; 
return total; 
int main()  
{ 
int n1,n2; 
cin>>n1>>n2; 
int sum1=sumofdivisors(n1); 
int sum2=sumofdivisors(n2); 
float a= sum1/n1; 
float b= sum2/n2; 
if (a==b) 
cout<<"friendly pair"; 
else 
cout<<"no"; 
} 

8)ABUNDANT NUMBER

Ex:- Abundant number 12 having a proper divisor is 1, 2, 3, 4, 6  
The sum of these factors is 16 it is greater than 12  
So it is an Abundant number 
Some other abundant numbers:  
18, 20, 30, 70, 78, 80, 84, 90, 96, 100, 104, 108, 120 

#include <iostream> 
using namespace std; 
int sumofdivisors(int n) 
{ 
} 
int sum=0; 
for (int i=1;i<n;i++)  //divisors should be less than the number   
if (n%i==0) 
sum+=i; 
return sum; 
int main() 
{ 
} 
int n; 
cin>>n; 
int a=sumofdivisors(n); 
if (a>n) 
cout<<"abundant number"; 
else 
cout<<"no";
